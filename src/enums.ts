export enum LAUNCH_MODE_ENUM {
  telegram_mini_app = 'telegram',
  test_proxy = 'test_proxy',
  test_browser = 'test_browser',
}

export enum LAUNCH_TELEGRAM_MODE_ENUM {
  run_automator = 'run_automator',
  add_account = 'add_account',
  import_account_from_csv = 'import_account_from_csv',
}

export enum CHAIN_ENUM {
  etherium = 'etherium',
}
